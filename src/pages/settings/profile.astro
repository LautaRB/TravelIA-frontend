---
import Layout from "../../layouts/BaseLayout.astro";
---

<Layout title="Mi Perfil - TravelIA">
  <div class="min-h-screen bg-ocean-50 relative">
    
    <header class="bg-white px-6 pt-12 pb-4 shadow-sm sticky top-0 z-10 flex items-center gap-4">
      <a href="/dashboard/dashboard/index.html" class="p-2 -ml-2 text-ocean-600 hover:bg-ocean-50 rounded-full transition">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
        </svg>
      </a>
      <h1 class="text-xl font-bold text-ocean-900">Mi Perfil</h1>
    </header>

    <main class="p-6 space-y-6">
      
      <section class="bg-white rounded-3xl p-6 shadow-sm shadow-ocean-100 flex flex-col items-center text-center border border-ocean-50">
        <div class="relative mb-4">
          <div class="w-24 h-24 rounded-full bg-ocean-100 border-4 border-white shadow-md overflow-hidden">
            <img 
              src="https://api.dicebear.com/7.x/avataaars/svg?seed=Felix" 
              alt="Avatar" 
              class="w-full h-full object-cover"
            />
          </div>
          <button class="absolute bottom-0 right-0 bg-ocean-600 text-white p-2 rounded-full hover:bg-ocean-700 transition shadow-sm">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 9a2 2 0 012-2h.93a2 2 0 001.664-.89l.812-1.22A2 2 0 0110.07 4h3.86a2 2 0 011.664.89l.812 1.22A2 2 0 0018.07 7H19a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V9z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 13a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
          </button>
        </div>
        
        <h2 id="profile-name" class="text-xl font-bold text-ocean-900">Viajero TravelIA</h2>
        <p id="profile-email" class="text-sm text-ocean-400">usuario@ejemplo.com</p>
      </section>

      <section class="space-y-3">
        <h3 class="text-xs font-bold text-ocean-400 uppercase tracking-wider ml-2">Cuenta</h3>
        
        <button class="w-full bg-white p-4 rounded-2xl shadow-sm flex items-center justify-between group active:scale-[0.98] transition">
          <div class="flex items-center gap-4">
            <div class="bg-ocean-50 p-2 rounded-lg text-ocean-600">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
              </svg>
            </div>
            <span class="text-ocean-800 font-medium">Datos Personales</span>
          </div>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-ocean-300 group-hover:text-ocean-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </button>

        <button class="w-full bg-white p-4 rounded-2xl shadow-sm flex items-center justify-between group active:scale-[0.98] transition">
          <div class="flex items-center gap-4">
            <div class="bg-ocean-50 p-2 rounded-lg text-ocean-600">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
            </div>
            <span class="text-ocean-800 font-medium">Configuración</span>
          </div>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-ocean-300 group-hover:text-ocean-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </button>
      </section>

      <section class="pt-4">
        <button 
          id="logoutBtn" 
          class="w-full border border-red-200 bg-red-50 text-red-600 font-bold py-3 rounded-xl hover:bg-red-100 active:scale-[0.98] transition flex items-center justify-center gap-2"
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
          </svg>
          Cerrar Sesión
        </button>
      </section>

    </main>
  </div>
</Layout>

<script>
  import { Capacitor } from "@capacitor/core";
  import { GoogleAuth } from "@codetrix-studio/capacitor-google-auth";
  import { logout } from "../../services/api/authAPI"; // Asegura la ruta correcta

  function getCookie(name: string): string | null {
      const value = `; ${document.cookie}`;
      const parts = value.split(`; ${name}=`);
      if (parts.length === 2) return parts.pop()?.split(';').shift() || null;
      return null;
  }

  const logoutBtn = document.getElementById("logoutBtn");

  if (logoutBtn) {
    logoutBtn.addEventListener("click", async () => {
      logoutBtn.innerHTML = `<span class="animate-pulse">Cerrando sesión...</span>`;
      (logoutBtn as HTMLButtonElement).disabled = true;

      const accessToken = getCookie('authToken');
      const refreshToken = getCookie('refreshToken');

      if (accessToken && refreshToken) {
          console.log("Cerrando sesión en servidor...");
          await logout(accessToken, refreshToken);
      }

      if (Capacitor.isNativePlatform()) {
        try {
            GoogleAuth.initialize({
                clientId: "360414482088-rtp2h0ao33g08ovo4fa2q6rk0c2n1aos.apps.googleusercontent.com",
                scopes: ["profile", "email"],
                grantOfflineAccess: true
            });
            await GoogleAuth.signOut();
        } catch (e) {
            console.error("Error logout nativo:", e);
        }
      }

      document.cookie = "authToken=; path=/; max-age=0; secure; samesite=strict";
      document.cookie = "refreshToken=; path=/; max-age=0; secure; samesite=strict";

      window.location.href = "/login/index.html";
    });
  }
</script>